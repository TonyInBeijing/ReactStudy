import './App.css';

import RenderProps from "./components/RenderProps";

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>React Playground</p>
        <RenderProps />
      </header>
    </div>
  );
}

export default App;
